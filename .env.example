# Optional Graph APIs for live follower counts on /#plataformas.
# Without these, the site scrapes public Instagram/TikTok pages.
# If both fail, counts render as "—" — never a fake number.

INSTAGRAM_ACCESS_TOKEN=
INSTAGRAM_USER_ID=
TIKTOK_ACCESS_TOKEN=
